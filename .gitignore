# Secrets — never commit. .env holds live Serper/Anthropic keys.
.env
.env.*
!.env.example

# Virtualenv
.venv/
venv/

# Caches: search responses, fetched pages, Claude responses, relation store
.artemis-cache/
*.db

# Exports and reports regenerated from the relation store. Several MB each, and
# derived rather than source — the same reason the cache above is not tracked.
cached_connections*.csv
seed_import_report.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editor / OS
.DS_Store
.idea/
.vscode/
